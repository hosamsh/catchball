# Create Demo Note

Work only inside `starter-output`.

Create the folder if it does not exist.

Add `starter-output/hello.md` with:
- first line: `# Hello`
- one short sentence saying `catchball runs tasks in filename order.`

Do not change anything else.

Keep it small and readable.